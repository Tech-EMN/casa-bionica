# Railway deployment configuration

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"

[service]
name = "casa-bionica-backend"

[env]
APP_ENV = "production"
LOG_LEVEL = "INFO"
